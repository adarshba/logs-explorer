# Example .env file for Rust backend
# Copy this to .env and fill in your values

# AI Provider Configuration
# Choose your provider: openai, azure-openai
AI_PROVIDER=openai

# OpenAI Configuration (if using OpenAI directly)
OPENAI_API_KEY=sk-your-openai-key-here
OPENAI_MODEL=gpt-4o-mini

# Azure OpenAI Configuration (if using Azure)
# Set AI_PROVIDER=azure-openai and configure these:
AZURE_OPENAI_API_KEY=your-azure-api-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Alternative: Generic AI configuration
AI_MODEL=gpt-4o-mini

# Optional: Pre-configure Elasticsearch
# (Can also configure via frontend settings UI)
# ELASTICSEARCH_URL=http://localhost:9200
# ELASTICSEARCH_USERNAME=elastic
# ELASTICSEARCH_PASSWORD=changeme

# Optional: Pre-configure OpenObserve
# OPENOBSERVE_URL=http://localhost:5080
# OPENOBSERVE_USERNAME=root@example.com
# OPENOBSERVE_PASSWORD=yourpassword
# OPENOBSERVE_ORG=default

# Logging Level (optional)
# RUST_LOG=info,zeteo_backend=debug
